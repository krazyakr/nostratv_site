version: '2'
services:
  web:
    build: .
    command:
      - node
      - "--inspect-brk=0.0.0.0"
      - "."
    volumes:
      - ./src/:/usr/app/
      - /usr/app/node_modules
    ports:
      - "3000:3000"
      - "9229:9229"